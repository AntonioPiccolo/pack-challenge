# Test environment variables
NODE_ENV=test
DB_NAME=pack_challenge
DB_USER=pack_user
DB_PASS=pack_password
DB_HOST=localhost
DB_PORT=5432

# API Key for testing
API_KEY=pack-challenge-api-key

# AWS S3 Configuration (not used in test mode)
AWS_REGION=us-east-1
S3_BUCKET_NAME=test-bucket